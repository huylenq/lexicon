# System: <project name>

> The cold layer. Things code can't express well: vocabulary, invariants, bounded contexts, and the "why"s.
> Keep this under ~500 lines. If it grows past that, something here probably belongs in code or in an ADR.
> Update via proposals in `lexicon/plans/_proposals/`, not by direct edits during work.

---

## Purpose

<One paragraph: what this system does, for whom. Plain language. Avoid jargon that isn't yet defined below.>

## Glossary

> The ubiquitous language. Every significant noun and verb in this system. Each term should appear in code with the same name. Counter-examples are encouraged — they disambiguate faster than definitions alone.

- **<Term>**: <definition>. NOT to be confused with <similar term>.
- **<Term>**: <definition>.

## Bounded contexts

> If the system has more than one context (e.g., training vs. inference, customer-facing vs. internal), name them and define their boundaries. The same word can mean different things in different contexts; flag that explicitly.

### <Context name>

<What lives in this context. What concepts are owned here.>

### <Other context name>

<...>

### Boundary rules

- <Rule>: e.g., "The Inference context never writes to the training data store."
- <Rule>: e.g., "Customer-facing context only knows about anonymized identifiers."

## Invariants

> Properties that must hold across the system. These are global rules, not per-module. Code may enforce them locally; this section names them so they don't get violated in module B while fixing module A.

- **<Invariant name>**: <plain-language statement>. <Why it matters; what breaks if it's violated.>
- **<Invariant name>**: <...>

## Architecture seams

> Where the system is allowed to be cut. Module boundaries that aren't mechanically enforced but should be respected.

- <Description of a seam>
- <Description of a seam>

## Decisions worth knowing

> Pointers to ADRs in `lexicon/decisions/` that explain choices a reader of `system.md` would want to know the reasoning for. Brief here; full reasoning lives in the ADR.

- <ADR-NNNN: short title> — <one-line summary of what it's about>

## Things deliberately not specified

> Resist the urge to fill this section reflexively. But if there are things readers might *expect* this doc to cover, and the answer is "we haven't decided yet" or "it's intentionally code-only," noting that here saves time.

- <Topic>: <why it's not specified>
