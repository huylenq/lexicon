# System: <project name>

## Purpose

<One paragraph: what this system does, for whom.>

## Glossary

- **<Term>**: <definition>. NOT to be confused with <similar term>.
- **<Term>**: <definition>.

## Bounded contexts

### <Context name>

<What lives in this context. What concepts are owned here.>

### <Other context name>

<...>

### Boundary rules

- <Rule>: e.g., "The Inference context never writes to the training data store."
- <Rule>: e.g., "Customer-facing context only knows about anonymized identifiers."

## Invariants

- **<Invariant name>**: <plain-language statement>. <Why it matters; what breaks if it's violated.>
- **<Invariant name>**: <...>

## Architecture seams

- <Description of a seam>
- <Description of a seam>

## Design system

### Tokens (canonical sources)

- Color: <path to canonical token file>
- Spacing: <path>
- Typography: <path>
- Radii / shadows / motion: <path>

### Component vocabulary

- **<Component>**: <role>. Variants: <list>. NOT to be confused with <similar>.

### Layout primitives

- **<Primitive>**: <when to use>.

### Surfaces & regions

- **<Surface name>** (`<route / screen-id / window>`):
  - **<Region name>** — *Component*: `<Import>` (`<path>`). <One-line role.>
  - **<Region name>** — *Inline*: `<file>:<lineStart>–<lineEnd>`. <One-line role.>

### Interaction patterns

- **<Pattern>**: use for <case>; never for <case>.

### Accessibility invariants

- **<Invariant>**: e.g., "every interactive element has a visible focus state". Why: <consequence of violation>.

## Decisions worth knowing

- <ADR-NNNN: short title> — <one-line summary of what it's about>

## Things deliberately not specified

- <Topic>: <why it's not specified>
