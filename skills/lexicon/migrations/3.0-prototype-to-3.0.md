# Schema 3.0-prototype to 3.0

The prototype's domain, C4, relationship, and flow XML shapes are retained. Change the root schema to `3.0`; preserve the project ID, every item, canonical nesting, annotation, code link, and ordered step. Keep step IDs local to their flow and preserve them across any requested reorder.

No viewpoint records, step registry, participant aliases, or new geometry are needed. Conditions and outcomes remain prose annotations; order means interaction order, not duration or completion. Do not rewrite the model merely to remove “prototype” from its human names or IDs. Follow README.md for preservation, validation, and delivery.
