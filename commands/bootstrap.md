---
description: Set up lexicon in this project — draft the cold-layer YAML from existing docs and code, then run the distillation interview.
argument-hint: ""
---

Invoke the `lexicon` skill with subcommand `adopt`.

The user wants to bootstrap lexicon in this project: scan existing docs and code, draft a first-cut `lexicon/system.yaml` and per-context files, archive ADR-shaped docs and lift their content into `rationale:` fields, then walk the user through distillation one decision at a time.
